 @using Project.Data.ExtendedDBEntities
@*@using Microsoft.Extensions.Caching.Memory
@using Project.Core.Extension
@using Project.Models.AdminModel *@
@inject Microsoft.AspNetCore.Identity.UserManager<DerivedIdentityUser> _userManager;
@* @inject Project.Services.IService.IMenuPermissionService _menuPermissionService; *@
@* @inject IMemoryCache _cache; *@
@{
    // List<MenuViewModel> menus = new List<MenuViewModel>();
    var user = await _userManager.GetUserAsync(User);
    List<string> roles = _userManager.GetRolesAsync(user).Result.ToList();
    // if (!_cache.TryGetValue(CacheKeys.UserMenu, out menus))
    // {
    //     menus = _menuPermissionService.GetMenuList(0);
    //     _cache.Set(CacheKeys.UserMenu, menus);
    // }
}

<header class="main-nav">
    <div class="logo-wrapper logo-section">
        <a data-original-title="" title="" class="logo-1" href="#">
            <img class="img-fluid" src="~/images/petasure_logo.svg" alt="Project" data-original-title="" title="" />
        </a>
    </div>
    <nav>
        <div class="main-navbar">
            <div id="mainnav">
                <ul class="nav-menu navbar-scrollbar" id="boxscroll">
                  
                    <li class="nav-item dropdown menu-icons">
                        <a id="a1" class="nav-link menu-title " href="~/Admin/dashboard">
                            <span class="sm-icon"></span>
                            <span class="menu-txt">Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item dropdown menu-icons">
                        <a id="a2" class="nav-link menu-title " href="~/Admin/UserManagement/SubUser">
                            <span class="sm-icon"></span>
                            <span class="menu-txt">Sub User</span>
                        </a>
                    </li>
                    <li class="nav-item dropdown menu-icons">
                        <a id="a3" class="nav-link menu-title " href="~/Admin/UserManagement/Merchant">
                            <span class="sm-icon"></span>
                            <span class="menu-txt">Merchant</span>
                        </a>
                    </li>
                    @if (roles.FirstOrDefault() == "Admin")
                    {
                        <li class="nav-item dropdown menu-icons">
                            <a id="a3" class="nav-link menu-title " href="~/Admin/Settings">
                                <span class="sm-icon"></span>

                                <i class="fas fa-gear"></i>

                                <span class="menu-txt">Settings</span>
                            </a>
                        </li>
                    }
                    
                </ul>
            </div>
        </div>

    </nav>
</header>